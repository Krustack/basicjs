git init สร้าง repository
git config user.name "ชื่อ" กำหนดชื่อ
git config user.email "email" กำหนด email
git remote ชื่อbrach ชื่อgithub repository
git add . เพิ่มไฟล์ที่ต้องการจะcommitโดยเลือกไฟล์ที่มีการupdateทั้งหมด
commit -m "ชื่อcommit" สร้างcommit
git push ส่งขึ้นgithub
